@model IEnumerable<SistemaSase.Models.tblQntPosto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PostVigilante)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PostPorteiro)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PostVigia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PostVigilante)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PostPorteiro)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PostVigia)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
